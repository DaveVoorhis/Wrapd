Run this after org.reldb.wrapd.tests.database.setup, to make use of code generated by the setup phase.

This also includes tests that are self-contained and require no separate setup or teardown phase.